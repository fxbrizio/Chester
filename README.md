# Proyecto corto

Mini proyecto web de contador con HTML, CSS y JavaScript.

## Ejecutar

Abre el archivo index.html en tu navegador o usa un servidor simple:

```bash
python -m http.server 8000
```

Luego entra a http://localhost:8000
